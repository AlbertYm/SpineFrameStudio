# 上传与修改规则

## 上传范围

只提交本工具源码、构建脚本、必要文档及不含私人数据的测试代码。禁止提交原始视频、GIF、图片序列、Spine 工程、工作素材、用户输出、私人截图、日志、聊天归档、API key、token、Cookie、配置凭据、临时目录和历史备份。不要在项目素材根目录执行 `git add .`。

EXE、FFmpeg 和 ZIP 不进入 Git 历史，完整可运行 ZIP 作为 GitHub Release 附件。不要用 Git LFS 绕开本仓库规则。必须核实第三方组件的许可、版本、构建来源及对应源码获取方式后再分发；禁止将它们声称为本项目所有。

## 修改流程

1. Fork 仓库或在有写权限的仓库创建 `fix/简短描述`、`feat/简短描述`、`docs/简短描述` 分支。
2. 开始前 `git status --short`，保留他人未提交的修改。一次提交解决一个问题，优先最小范围修改。
3. 修改 `src/` 中实际运行源码；不要修改本机旧发布目录后误以为仓库已更新。
4. 使用可公开、合成或自己有权使用的小样本验证。不得用上传工作素材的方式复现问题。
5. `git diff --check`，检查 `git diff` 和 `git diff --cached`，确认没有素材、秘密及无关变更。
6. 提交信息写清用途，例如 `fix: 修复 GIF 抽帧数量`。推送自己的分支后创建 Pull Request；描述问题、结果、测试环境、已执行测试和未验证范围。
7. 维护者审查并合并。禁止直接覆盖已发布 tag、强推共享 main、删除历史 Release。提交的项目代码与文档须由你拥有或已获授权，并同意按本项目 MIT 许可提供。第三方代码保留原许可和版权声明，不得直接改标 MIT。

```powershell
git switch -c fix/example
# 修改并验证
git diff --check
git add src/具体文件.ps1
git diff --cached
git commit -m "fix: 描述实际修复"
git push -u origin fix/example
```

## 发布规则

- `src/version.json` 是单一版本依据，发布新版本时同步 executable、fileVersion、文档与版本号，禁止覆盖已交付 ZIP。
- 公开发布必须使用 `tools/build.ps1 -ForPublicRelease`；当前第三方材料未闭合时脚本会拒绝，不得通过修改状态字段、移除检查或发布普通本地构建绕过审核。具体缺项见 `compliance/README.md`。
- 构建脚本使用独立 dist 目录。先检查 PowerShell 语法，再在全新中文和空格目录解压完整 ZIP，校验清单及 EXE 版本、启动、真实短视频和 GIF 抽帧；修改涉及 GUI 时验证相关交互、滚动、长名称、窗口缩放和 DPI。
- 必须报告开发机验证与目标机验证的区别；历史记录明确注明日期，不将旧测试写成本次已通过。
- 发布包包含全部运行依赖、最终用户说明、SHA-256 清单，以及适用第三方许可与源码说明。检查依赖许可和对应源码分发要求；不能只附一个项目通用许可证覆盖依赖。
- 使用 `v版本号` tag，Release 中写功能、修复、依赖、未签名状态、已知限制、测试及回退方法，上传完整 ZIP 和 ZIP 的 SHA-256。
- 先保留旧版，升级使用新目录；不得删除用户素材和输出。出现问题停止发布，修复后使用新版本号重新发布。

## 问题反馈

Issue 中写版本、Windows 版本、DPI、复现步骤、预期/实际结果和脱敏错误文字。不要上传私人素材或完整本机诊断日志。安全问题不要公开粘贴密钥，先撤销泄露凭据。
